apiVersion: v1
kind: Secret
metadata:
  name: controller-secrets
type: Opaque
stringData:
  # Blank triggers the insecure dev default in CaKeyEncryption; set to 32 base64 bytes for prod.
  ca_encryption_key: ""
