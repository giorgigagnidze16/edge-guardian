apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - secret.yaml
configMapGenerator:
  - name: keycloak-realm
    files:
      - edgeguardian-realm.json
generatorOptions:
  disableNameSuffixHash: true
