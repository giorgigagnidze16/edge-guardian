spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

edgeguardian:
  controller:
    mqtt:
      broker-url: tcp://localhost:1883
      client-id: test-controller
      topic-root: edgeguardian
