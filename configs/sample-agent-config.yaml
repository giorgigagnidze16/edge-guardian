# EdgeGuardian agent configuration.
# Place at /etc/edgeguardian/agent.yaml on the device.

device_id: rpi-gateway-01
controller_address: 192.168.1.100
controller_port: 8443

labels:
  role: gateway
  location: office

reconcile_interval_seconds: 30
log_level: info
data_dir: /var/lib/edgeguardian

# Authentication — set enrollment_token to enroll with an organization.
# After enrollment, the agent receives a device token and saves it to token_file.
# On subsequent startups the agent loads the device token from token_file.
auth:
  enrollment_token: ""        # e.g. egt_xxxxx (get from dashboard)
  token_file: ""              # default: {data_dir}/device-token

# MQTT broker configuration for telemetry and commands.
mqtt:
  broker_url: tcp://192.168.1.100:1883
  username: ""
  password: ""
  topic_root: edgeguardian
